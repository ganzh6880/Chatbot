const CoffeeManager = require("./coffeeManager");
describe("sanity",() =>{
    it('should pass',() => { 
        expect(0).toEqual(0);
        expect(4-2).toEqual(2);
    });
});

//module.export = CoffeeManager (export objects)
